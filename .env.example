# Required — text + vision tools run on Groq (fast, generous free tier).
# Get a free key (no credit card) at https://console.groq.com/keys
GROQ_API_KEY=

# Not required — the real AI Image Generator tool calls Pollinations.ai
# directly from the browser, no key needed for that one.
